# Compatibility entry point. Package installation and caching are owned by the
# shared font module; Home Manager removes the old managed XDG links on switch.
_: { typography.optionalEmoji.enable = true; }
